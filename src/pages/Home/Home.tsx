import { Box } from "@mui/material";
import { CardContainer, Header } from "../../components";

const Home = () => (
   <Box sx={{ m: "50px 75px", px: 0 }}>
      <Header />
      <CardContainer />
   </Box>
);
export default Home;
